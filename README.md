A classical vision pipeline for deriving shape, color, and position from stereo images and converting them into sorting trajectories.  
  
To run:  
```
git clone https://github.com/aidan073/StereoSort.git  
git submodule update --init --recursive  
```